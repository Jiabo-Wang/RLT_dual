__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
build/
dist/
*.egg-info/
outputs/
pretrained/
# pi0.5 SFT 权重: 8.8 GB, 单文件 9.35 GB 超 GitHub 100 MB 上限。
# 走 HF hub 或内部存储分发, 不入 git。
pretrained_model/
cache/
wandb/
data/
.DS_Store

# CRP native runtime — machine-licensed and vendor-proprietary, never publish.
# license.key is bound to this machine's identifier; libRobotService.so and
# CrpRobotPy.so are Crobotp's closed-source SDK. See README_crp_dual.md to
# reassemble them locally. Paths are named explicitly rather than globbing *.so,
# which would also swallow any compiled extension built inside the repo.
license.key
license-*.key
identifier.id
third_party/CrpRobotPy/
src/evo_rlt/adapters/crp/getui_probe/libRobotService.so
src/evo_rlt/adapters/crp/getui_probe/crp_getui_probe
/license.key
